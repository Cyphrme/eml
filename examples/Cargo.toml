[package]
name          = "eml-examples"
version       = "0.0.0"
description   = "Worked examples / integration tests for the EML stack"
edition       = "2024"
rust-version  = "1.85"
publish       = false

[lib]
path = "src/lib.rs"

[dev-dependencies]
spine      = { path = "../spine" }
polydigest = { path = "../polydigest" }
eml        = { path = "../eml" }
sha2       = "0.10"
smol      = "2.0"
