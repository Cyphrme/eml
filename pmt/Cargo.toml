[package]
name          = "pmt"
version       = "0.1.0"
description   = "Compatibility facade over the re-layered spine + epoch crates (transitional; consumers are being re-pointed at spine/epoch directly)"
edition       = "2024"
rust-version  = "1.85"
license-file  = "../LICENSE"
repository    = "https://github.com/Cyphrme/eml"

[dependencies]
# The facade re-exports both halves of the split kernel.
spine = { path = "../spine" }
epoch = { path = "../epoch" }
